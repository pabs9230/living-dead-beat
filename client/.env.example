# Example environment variables for the client (Vite)
# Either supply a full WebSocket URL:
VITE_WS_URL=ws://localhost:4041

# Or supply just the port and the client will connect to the same host:
# VITE_WS_PORT=4041
